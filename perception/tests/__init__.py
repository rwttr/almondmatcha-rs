"""Test package for `rover_perception`.

Exists (rather than leaving `tests/` import-mode-implicit) so that
`tests.oracle` is importable as a proper subpackage — see
`tests/oracle/README.md` for what that package is and why it must stay
frozen.
"""
